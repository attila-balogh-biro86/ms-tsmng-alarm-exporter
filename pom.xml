<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns="http://maven.apache.org/POM/4.0.0"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.1</version>
        <relativePath/>
    </parent>
    <groupId>com.cts.ms.tsmng.alarm.exporter</groupId>
    <artifactId>ms-tsmng-alarm-exporter</artifactId>
    <version>0.1.0</version>
    <name>MsTsmngAlarmExporter</name>
    <description>
        TSMNG alarm exporter — emits custom business metrics derived from MongoDB Atlas state
        into Azure Monitor via the central OpenTelemetry collector. Owns the polling alarms and
        receives Atlas Database Trigger webhooks for the event-driven alarms.
    </description>
    <packaging>jar</packaging>

    <properties>
        <project.artifact.name>${project.artifactId}</project.artifact.name>

        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <java.version>21</java.version>
        <maven.compiler.source>${java.version}</maven.compiler.source>
        <maven.compiler.target>${java.version}</maven.compiler.target>
        <jgitflow-maven-plugin.version>1.0-m5.1</jgitflow-maven-plugin.version>
        <docker-maven-plugin.version>1.2.1</docker-maven-plugin.version>

        <!-- JACOCO CONFIGURATION -->
        <plugin.jacoco.version>0.8.14</plugin.jacoco.version>
        <unit.testing.element>BUNDLE</unit.testing.element>
        <unit.testing.limit.counter>LINE</unit.testing.limit.counter>
        <unit.testing.limit.value>COVEREDRATIO</unit.testing.limit.value>
        <unit.testing.limit.minimum>0.80</unit.testing.limit.minimum>

        <!-- SPRINGDOC -->
        <springdoc.version>2.5.0</springdoc.version>

        <!-- CTS -->
        <arc-architecture.version>1.0.0</arc-architecture.version>
        <arc-starter-exception-handler.version>1.0.0</arc-starter-exception-handler.version>
        <ctsaudit.version>0.0.1</ctsaudit.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.cts.architecture</groupId>
                <artifactId>arc-architecture-bom</artifactId>
                <version>${arc-architecture.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>io.opentelemetry.instrumentation</groupId>
                <artifactId>opentelemetry-instrumentation-bom</artifactId>
                <version>2.11.0</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>${springdoc.version}</version>
        </dependency>

        <!-- Testing -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- CTS starters (company standard) -->
        <dependency>
            <groupId>com.cts.architecture</groupId>
            <artifactId>arc-starter-mongodb</artifactId>
        </dependency>
        <dependency>
            <groupId>com.cts.architecture</groupId>
            <artifactId>arc-starter-kubernetes-discovery</artifactId>
        </dependency>
        <dependency>
            <groupId>com.cts.architecture</groupId>
            <artifactId>arc-starter-logging</artifactId>
            <exclusions>
                <exclusion>
                    <groupId>io.micrometer</groupId>
                    <artifactId>micrometer-tracing-bridge-brave</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>com.cts.architecture</groupId>
            <artifactId>arc-starter-vault-config</artifactId>
        </dependency>
        <dependency>
            <groupId>com.cts.architecture</groupId>
            <artifactId>arc-starter-security-azure-apim</artifactId>
        </dependency>
        <dependency>
            <groupId>com.cts.architecture</groupId>
            <artifactId>arc-starter-exception-handler</artifactId>
            <version>${arc-starter-exception-handler.version}</version>
        </dependency>

        <dependency>
            <groupId>com.cts</groupId>
            <artifactId>ctsaudit</artifactId>
            <version>${ctsaudit.version}</version>
        </dependency>

        <!-- Logging: Logback JSON -->
        <dependency>
            <groupId>net.logstash.logback</groupId>
            <artifactId>logstash-logback-encoder</artifactId>
            <version>7.4</version>
        </dependency>

        <!--
            The OpenTelemetry SDK is installed at runtime by the Java agent
            injected via the OTel Operator (see deployment chart annotation
            instrumentation.opentelemetry.io/inject-java). We only depend on
            the API at compile time; GlobalOpenTelemetry.get() returns the
            agent's instance in AKS, and OpenTelemetry.noop() locally.
        -->
        <dependency>
            <groupId>io.opentelemetry</groupId>
            <artifactId>opentelemetry-api</artifactId>
        </dependency>

        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-core</artifactId>
        </dependency>

    </dependencies>

    <distributionManagement>
        <repository>
            <id>CTS-TSMNG-feed</id>
            <url>https://pkgs.dev.azure.com/cintratollservices/318af6d1-a076-424f-8bad-44ffbaece3eb/_packaging/CTS-TSMNG-feed/maven/v1</url>
        </repository>
    </distributionManagement>

    <repositories>
        <repository>
            <id>CTS-TSMNG-feed</id>
            <url>https://pkgs.dev.azure.com/cintratollservices/318af6d1-a076-424f-8bad-44ffbaece3eb/_packaging/CTS-TSMNG-feed/maven/v1</url>
            <releases>
                <enabled>true</enabled>
            </releases>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
        </repository>
    </repositories>

    <build>
        <plugins>
            <plugin>
                <groupId>external.atlassian.jgitflow</groupId>
                <artifactId>jgitflow-maven-plugin</artifactId>
                <version>${jgitflow-maven-plugin.version}</version>
                <configuration>
                    <flowInitContext>
                        <masterBranchName>master</masterBranchName>
                        <developBranchName>develop</developBranchName>
                        <featureBranchPrefix>feature/</featureBranchPrefix>
                        <releaseBranchPrefix>release/</releaseBranchPrefix>
                        <hotfixBranchPrefix>hotfix/</hotfixBranchPrefix>
                    </flowInitContext>
                    <noDeploy>true</noDeploy>
                    <squash>true</squash>
                    <scmCommentPrefix>[RELEASE]</scmCommentPrefix>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <release>${java.version}</release>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>start-spring-boot</id>
                        <phase>pre-integration-test</phase>
                        <goals>
                            <goal>start</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>stop-spring-boot</id>
                        <phase>post-integration-test</phase>
                        <goals>
                            <goal>stop</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>com.spotify</groupId>
                <artifactId>docker-maven-plugin</artifactId>
                <version>${docker-maven-plugin.version}</version>
                <configuration>
                    <imageName>containerRegistry.azurecr.io/${project.artifactId}</imageName>
                    <dockerDirectory>docker</dockerDirectory>
                    <resources>
                        <resource>
                            <targetPath>/</targetPath>
                            <directory>${project.build.directory}</directory>
                            <include>${project.build.finalName}.jar</include>
                        </resource>
                    </resources>
                    <imageTags>
                        <imageTag>${project.version}</imageTag>
                        <imageTag>latest</imageTag>
                    </imageTags>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${plugin.jacoco.version}</version>
                <executions>
                    <execution>
                        <id>before-unit-test-execution</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>after-unit-test-execution-report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${project.reporting.outputDirectory}/jacoco-unit-test-coverage-report</outputDirectory>
                        </configuration>
                    </execution>
                    <execution>
                        <id>coverage-check</id>
                        <goals>
                            <goal>check</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <rule>
                                    <element>${unit.testing.element}</element>
                                    <limits>
                                        <limit>
                                            <counter>${unit.testing.limit.counter}</counter>
                                            <value>${unit.testing.limit.value}</value>
                                            <minimum>${unit.testing.limit.minimum}</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-maven-plugin</artifactId>
                <version>1.5</version>
                <executions>
                    <execution>
                        <id>generate-api-docs</id>
                        <phase>integration-test</phase>
                        <goals>
                            <goal>generate</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <skip>false</skip>
                    <apiDocsUrl>http://localhost:8080/v3/api-docs</apiDocsUrl>
                    <outputFileName>openapi.json</outputFileName>
                    <outputDir>${project.build.directory}</outputDir>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
